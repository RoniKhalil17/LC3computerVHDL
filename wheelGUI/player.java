/* 
public class player {
    /* 
    JButton button = new JButton("player1");
    button.setBounds(50, 100, 100, 30);
    button.setBackground(Color.BLACK);
    button.setVisible(true); 
    button.setOpaque(true); // Set to true to make the background color visible
    cirkelPanel.setLayout(null); // Set layout to null to use absolute positioning
    cirkelPanel.add(button); // Add the button to the panel
    JPanel buttonPanel = new JPanel();
    buttonPanel.setBackground(Color.BLACK);
    buttonPanel.setBounds(50, 200, width, height);
    buttonPanel.setLayout(null); // Set layout to null for absolute positioning
    // Add a label with text
    javax.swing.JLabel label = new javax.swing.JLabel("SCORE: "); // Create a label with initial text
    label.setForeground(Color.BLACK); // Set text color to white
    label.setBounds(50, 130, 100, 30); // Set position and size of the label
    buttonPanel.add(label); // Add the label to the button panel
    frame.add(buttonPanel); // Add the button panel to the frame
    label.setOpaque(true); // Make the label opaque to see the background
    label.setBackground(Color.WHITE); // Set the background color of the label
    label.setBorder(javax.swing.BorderFactory.createLineBorder(Color.WHITE, 2)); // Add a white border around the label

}
*/